# Environment
.venv/
__pycache__/
*.pyc

# Model weights (download separately, see README)
checkpoints/

# Datasets (download/generate separately, see README)
LEVIR-CD256/
LEVIR-CD256-raw/

# Test/train run outputs (regenerated by running the pipeline, not source)
experiments/
*.pt

# SpaceNet MVOI exploration (separate, ongoing work not yet part of this fork)
*.tar.gz
Atlanta_nadir*/
*.tif
*_rgb8.png
check_tile.py
find_matching_tile.py
inspect_and_convert_tile.py