name: diffregcd
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch>=1.11
  - torchvision
  - pytorch-cuda=11.8
  - numpy
  - scipy
  - pandas
  - tqdm
  - pip
  - pip:
      - opencv-python
      - pillow
      - einops
      - kornia
      - lmdb
      - thop
      - flow_vis
      - tensorboardX
      - wandb
      # - xformers   # optional: memory-efficient attention
